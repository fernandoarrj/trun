matrix:
  include:
    - language: python
      python:
        - "3.6"
      install:
        - pip install docker-compose
      script:
        - python manage.py test
      services:
        docker

    - language: node_js
      node_js:
        - "13.12.0"
      before_install:
        - cd frontend 
      install:
        npm install
      script:
        - npm test
      services:
        docker
